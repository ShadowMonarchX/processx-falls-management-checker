# Assumptions

The policy text is the source of truth for validation requirements.

The sample workbook is treated as the behavioral baseline for acceptable flag wording and severity.

Day labels in the workbook are expected to follow the pattern `Day 1`, `Day 2`, and `Day 3`.

The output workbook is appended in place within the existing output sheets and no new worksheets are created.

