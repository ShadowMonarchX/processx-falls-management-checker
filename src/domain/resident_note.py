from src.core.models import ResidentNoteModel

# This class represents a resident note, which is a specific type of data model that extends the base ResidentNoteModel.
# It can be used to create instances of resident notes with the same structure and properties defined in the ResidentNoteModel, allowing for easy integration and manipulation of resident note data within the application.


class ResidentNote(ResidentNoteModel):
    pass
